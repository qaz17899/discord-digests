# 複製成 .env，放在資料根目錄（DM_HOME，預設就是這個目錄）。
# discordwatch 啟動時會讀這個檔；真正的環境變數會蓋過它。
# .env 不進版控。

# 讀訊息用的 user token，必填。
DISCORD_TOKEN=

# 換附件連結用的 bot token，選填；沒填就不會去換。
DISCORD_BOT_TOKEN=

# 摘要模型的 API key，選填；端點和模型名稱放在 config.json。
DM_LLM_API_KEY=
